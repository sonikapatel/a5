<div style="float:left; padding-right:30px;">
<h2>Artists</h2>
<ul class="artists">
	{{#each model.artists as |artist|}}
		<li>{{#link-to 'artists.artist' artist.id}}{{artist.name}}{{/link-to}}  </li>
	{{/each}}
</ul>
</div>
{{outlet}}
